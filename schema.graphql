# import User from './generated/prisma.graphql'

type Query {
  user(id: ID!): User
}

type User {
    id: ID!
    username: String
    password: String
}

type Mutation {
  signup(username: String!): User!
}